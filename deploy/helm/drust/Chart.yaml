apiVersion: v2
name: drust
description: drust — multi-tenant SQLite BaaS, one isolated instance per group on k3s
type: application
version: 0.1.3
# MUST equal Cargo.toml's version — render_test.sh asserts it, and since
# values.yaml's image.tag defaults to this field, it is what `helm install`
# actually pulls. Bump both in the release commit.
appVersion: "1.58.1"
home: https://github.com/KaelLim/drust
sources:
  - https://github.com/KaelLim/drust
