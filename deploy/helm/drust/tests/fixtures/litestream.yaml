ingress: { host: ls.example.test, controller: traefik }
storage: { enabled: false }
networkPolicy:
  allowInternetEgress: false
backup:
  litestream:
    enabled: true
  external:
    bucket: drust-backup
    endpoint: https://s3.backup.example.test
    accessKey: LSAK
    secretKey: LSSK
    destinationCIDRs: ["203.0.113.0/24"]   # egress OFF => scoped backup egress is required (fail-closed)
secrets: { create: true, adminPassword: pw }
